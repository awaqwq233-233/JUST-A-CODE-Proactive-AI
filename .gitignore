# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/

# Virtual Environment
.venv/
.venv.bak/
venv/
ENV/

# IDE / editor
.vscode/
.idea/
.cursor/
*.swp

# Caches & runtime output
.cache/
temp/
output/
*.log
temp_audio.wav

# Large model / media assets (do not commit to GitHub)
# 项目内 *.gguf 大脑/判断模型随仓库提供，但一律不进 Git（见下方全局 *.gguf 等规则）。
models/*
# qwen_tts 权重由 download_models.py 下载（约 4GB，可随时重拉），务必勿提交。
models/qwen_tts/
GenieData/
genie_assets/onnx/
genie_assets/*.mp3
genie_assets/*.wav
*.gguf
*.pt
*.onnx
*.bin
*.safetensors
ffmpeg.exe
# voices/ 下的参考音（如 silverwalf_voice.wav、zh_vo_Main_Linaxita_2_1_10_26.wav）
# 是 J.A.C. 音色核心资产、体积很小，有意保留、不忽略——按需 `git add voices/` 提交。
# 若日后想把参考音也排除，加 `voices/*.wav` 即可。

# PyInstaller build output
JAC_Prototype/
JAC_Prototype.spec

# Secrets & credentials (API keys, tokens, passwords)
.env
.env.*
!.env.example
*.pem
*.key
*.p12
credentials.json
secrets/
key
key.pub
**/api_keys.*
**/apikeys.*
*secret*.json
*credentials*.json

# Archives
*.zip
*.tar.gz
*.7z

# OS junk
.DS_Store
Thumbs.db

# Node (if ever added)
node_modules/

# Local AI assistant memory & config (do not commit to GitHub)
.workbuddy/

# Claude local config (do not commit)
.claude/

# Office temp/lock files (Word ~$ prefix)
~$*.docx
~$*.doc

# 第三方 Qwen3-TTS WebUI 一键包（用户临时放入，Windows 专用、含 9GB+ 捆绑 Python 与不完整模型缓存）
# 不是项目源码，禁止提交；其中的「银狼」参考音已提取到 voices/silverwolf_template_from_qwentts3.wav。
qwentts3/
