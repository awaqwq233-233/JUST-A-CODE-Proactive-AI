# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/

# Virtual Environment
.venv/
.venv.bak/
venv/
ENV/

# IDE / editor
.vscode/
.idea/
.cursor/
*.swp

# Caches & runtime output
.cache/
temp/
output/
*.log
temp_audio.wav

# Large model / media assets (do not commit to GitHub)
# 项目不再在仓库内保留任何模型文件：大脑由 LM Studio 管理、TTS 由 Voicebox 管理，
# 视觉检测 yolov8n.pt 由 ultralytics 首次运行时自动下载到项目根。下方规则仅作兜底，
# 防止任何意外的大体积模型/权重被误提交（历史上曾用 models/ 与 models/qwen_tts/）。
models/*
models/qwen_tts/
GenieData/
genie_assets/onnx/
genie_assets/*.mp3
genie_assets/*.wav
*.gguf
*.pt
*.onnx
*.bin
*.safetensors
ffmpeg.exe
# voices/ 下的参考音（silverwalf_voice.wav 为 J.A.C. 唯一音色，体积很小）
# 是 J.A.C. 音色核心资产，有意保留、不忽略——按需 `git add voices/` 提交。
# 若日后想把参考音也排除，加 `voices/*.wav` 即可。

# PyInstaller build output
JAC_Prototype/
JAC_Prototype.spec

# Secrets & credentials (API keys, tokens, passwords)
.env
.env.*
!.env.example
*.pem
*.key
*.p12
credentials.json
secrets/
key
key.pub
**/api_keys.*
**/apikeys.*
*secret*.json
*credentials*.json

# Archives
*.zip
*.tar.gz
*.7z

# OS junk
.DS_Store
Thumbs.db

# Node (if ever added)
node_modules/

# Local AI assistant memory & config (do not commit to GitHub)
.workbuddy/

# 用户手动维护的项目笔记（不进 GitHub，由用户手写，避免误发到开源仓库）
codinglog_by_awaqwq233/

# Claude local config (do not commit)
.claude/

# Office temp/lock files (Word ~$ prefix)
~$*.docx
~$*.doc

# 第三方 Qwen3-TTS WebUI 一键包（用户临时放入，Windows 专用、含 9GB+ 捆绑 Python 与不完整模型缓存）
# 不是项目源码，禁止提交；其中的「银狼」参考音已提取到 voices/silverwolf_template_from_qwentts3.wav。
qwentts3/
